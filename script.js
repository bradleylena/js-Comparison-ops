let score = 85;
let grade =
  score >= 90
    ? "A"
    : score >= 80
    ? "B"
    : score >= 70
    ? "C"
    : score >= 60
    ? "C"
    : "end";
console.log(grade);

let age = 19;
let canvote = age >= 18 ? "Yes" : "No";
console.log(canvote);

console.log(10 > 5 ? "Greater" : "Smaller");
